<%= form_for(@wellcome) do |f| %>
  <%=  %>
  <% if @wellcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wellcome.errors.count, "error") %> prohibited this wellcome from being saved:</h2>

      <ul>
      <% @wellcome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
