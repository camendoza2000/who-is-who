<%= form_for @user, :url => users_path, :html => { :multipart => true } do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.file_field :avatar %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>